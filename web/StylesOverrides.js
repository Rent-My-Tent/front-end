import { over } from "lodash";


export const accordion = {
    Content: {
        style: ({ $theme }) => {
          return {
            backgroundColor: $theme.colors.primaryB
          };
        }
    }
}